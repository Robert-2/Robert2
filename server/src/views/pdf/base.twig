<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>{% block title %}{% endblock %}</title>
    <style type="text/css">
        html {
            margin: 10mm 8mm;
            font-family: Helvetica, 'DejaVu Sans';
            font-size: 10pt;
        }
        body { margin: 0; }
        h1 { font-size: 13pt; margin: 0 0 3mm 0; }
        h2 { font-size: 12pt; margin: 0 0 3mm 0; }
        h3 { font-size: 11pt; margin: 0 0 3mm 0; }
        h4 { font-size: 10pt; margin: 0 0 3mm 0; }
        p { margin: 0 0 3mm 0; }
        ul, ol { margin: 2mm 0 3mm 0; }
        table {
            width: 100%;
            table-layout: auto;
            margin: 0 0 3mm 0;
            border-spacing: 0;
            border-collapse: collapse;
        }
        th, td {
            padding: 1mm 2mm;
            border-top: 1pt solid #888;
            border-left: 1pt solid #888;
            vertical-align: top;
        }
        th { text-align: center; }
        th:last-child, td:last-child { border-right: 1pt solid #888; }
        tr:last-child > td { border-bottom: 1pt solid #888; }
        .layout-table > tbody > tr > td { border: none; padding: 0; }
        .half { width: 50%; }
        .left { text-align: left; }
        .center { text-align: center; }
        .right { text-align: right; }
        .size12 { font-size: 12pt; }
        .size10 { font-size: 10pt; }
        .size8 { font-size: 8pt; }
        .size7 { font-size: 7pt; }
        .vspace2 { margin: 0; padding: 2mm 0; }
        .vspace5 { margin: 0; padding: 5mm 0; }
        .vspace10 { margin: 0; padding: 10mm 0; }
        .indent5 { padding-left: 5mm; }
        .indent10 { padding-left: 10mm; }
        .box {
            padding: 1mm;
            background-color: #eee;
            border-radius: 3pt;
        }
        .strong { font-weight: bold; }
        .light { color: #333; }
        .lighter { color: #666; }
        .changePage { page-break-before: always; }
    </style>
</head>
<body>
    {% block content %}{% endblock %}
</body>
</html>